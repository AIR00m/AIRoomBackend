input {
    kafka {
        bootstrap_servers => "kafka:29092"
        topics => ["exam-logs"]
        group_id => "exam-logs-consumer"
        client_id => "logstash-exam-logs"
        auto_offset_reset => "earliest"
        codec => "json"
    }
}

filter {
}

output {
    stdout {
        codec => rubydebug
    }

    elasticsearch {
        hosts => ["elasticsearch:9200"]
        index => "exam-logs-2025.08.30"
    }
}