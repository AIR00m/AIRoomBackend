input {
    kafka {
        bootstrap_servers => "kafka:29092"
        topics => ["app-logs"]
        group_id => "app-consumer"
        auto_offset_reset => "earliest"
        codec => "json"
    }
    kafka {
        bootstrap_servers => "kafka:29092"
        topics => ["user-activity"]
        group_id => "activity-consumer"
        auto_offset_reset => "earliest"
        codec => "json"
    }
    kafka {
        bootstrap_servers => "kafka:29092"
        topics => ["exam-events"]
        group_id => "exam-events-consumer"
        auto_offset_reset => "earliest"
        codec => "json"
    }
    kafka {
        bootstrap_servers => "kafka:29092"
        topics => ["exam-results"]
        group_id => "exam-results-consumer"
        auto_offset_reset => "earliest"
        codec => "json"
    }
    kafka {
        bootstrap_servers => "kafka:29092"
        topics => ["exam-analytics"]
        group_id => "exam-analytics-consumer"
        auto_offset_reset => "earliest"
        codec => "json"
    }
    kafka {
        bootstrap_servers => "kafka:29092"
        topics => ["secure-agent-logs"]
        group_id => "secure-agent-logs-consumer"
        auto_offset_reset => "earliest"
        codec => "json"
    }
    kafka {
        bootstrap_servers => "kafka:29092"
        topics => ["secure-agent-events"]
        group_id => "secure-agent-events-consumer"
        auto_offset_reset => "earliest"
        codec => "json"
    }
}

filter {
    if [@metadata][kafka][topic] == "app-logs" {
        json {
            source => "message"
            target => "parsed"
        }
        mutate {
            add_field => {
                "traceId" => "%{[parsed][traceId]}"
                "timestamp" => "%{[parsed][timestamp]}"
                "level" => "%{[parsed][level]}"
                "prefix" => "%{[parsed][prefix]}"
                "logMessage" => "%{[parsed][message]}"
                "args" => "%{[parsed][args]}"
                "executionTimeMs" => "%{[parsed][timeMs]}"
                "exception" => "%{[parsed][exception]}"
            }
            remove_field => ["message", "parsed"]
        }
    }

    if [@metadata][kafka][topic] == "user-activity" {
        json {
            source => "message"
            target => "parsed"
        }
        mutate {
            add_field => {
                "memberId" => "%{[parsed][memberId]}"
                "memberName" => "%{[parsed][memberName]}"
                "role" => "%{[parsed][role]}"
                "classroomNo" => "%{[parsed][classroomNo]}"
                "classroomStudentNo" => "%{[parsed][classroomStudentNo]}"
                "textbookNo" => "%{[parsed][textbookNo]}"
                "textbookTitle" => "%{[parsed][textbookTitle]}"
                "textbookSubject" => "%{[parsed][textbookSubject]}"
                "textbookGrade" => "%{[parsed][textbookGrade]}"
                "textbookPublisher" => "%{[parsed][textbookPublisher]}"
                "textbookSemester" => "%{[parsed][textbookSemester]}"
                "eventType" => "%{[parsed][eventType]}"
                "eventTime" => "%{[parsed][eventTime]}"
                "eventDate" => "%{[parsed][eventDate]}"
                "userAgent" => "%{[parsed][userAgent]}"
                "browserInfo" => "%{[parsed][browserInfo]}"
                "processedAt" => "%{[parsed][processedAt]}"
                "studentName" => "%{[parsed][memberName]}"
                "anomalyType" => "%{[parsed][eventType]}"
                "timestamp" => "%{[parsed][processedAt]}"
            }
            remove_field => ["message", "parsed"]
        }
        date {
            match => [ "eventTime", "ISO8601" ]
            target => "@timestamp"
        }
    }

    if [@metadata][kafka][topic] == "exam-events" {
        json {
            source => "message"
            target => "parsed"
        }
        mutate {
            add_field => {
                "examNo" => "%{[parsed][examNo]}"
                "studentNo" => "%{[parsed][studentNo]}"
                "classroomNo" => "%{[parsed][classroomNo]}"
                "eventType" => "%{[parsed][eventType]}"
                "timestamp" => "%{[parsed][timestamp]}"
                "examTitle" => "%{[parsed][examTitle]}"
                "questionNo" => "%{[parsed][questionNo]}"
                "actionData" => "%{[parsed][actionData]}"
            }
            remove_field => ["message", "parsed"]
        }
    }

    if [@metadata][kafka][topic] == "exam-results" {
        json {
            source => "message"
            target => "parsed"
        }
        mutate {
            add_field => {
                "examNo" => "%{[parsed][examNo]}"
                "studentNo" => "%{[parsed][studentNo]}"
                "classroomNo" => "%{[parsed][classroomNo]}"
                "totalQuestions" => "%{[parsed][totalQuestions]}"
                "correctAnswers" => "%{[parsed][correctAnswers]}"
                "score" => "%{[parsed][score]}"
                "completedAt" => "%{[parsed][completedAt]}"
                "timeSpent" => "%{[parsed][timeSpent]}"
                "answers" => "%{[parsed][answers]}"
            }
            remove_field => ["message", "parsed"]
        }
    }

    if [@metadata][kafka][topic] == "exam-analytics" {
        json {
            source => "message"
            target => "parsed"
        }
        mutate {
            add_field => {
                "examNo" => "%{[parsed][examNo]}"
                "questionNo" => "%{[parsed][questionNo]}"
                "difficultyLevel" => "%{[parsed][difficultyLevel]}"
                "correctRate" => "%{[parsed][correctRate]}"
                "averageTime" => "%{[parsed][averageTime]}"
                "unitNo" => "%{[parsed][unitNo]}"
                "analysisType" => "%{[parsed][analysisType]}"
                "timestamp" => "%{[parsed][timestamp]}"
            }
            remove_field => ["message", "parsed"]
        }
    }
    if [@metadata][kafka][topic] == "secure-agent-logs" {
        # AgentIngestController가 넣은 timestamp(ISO8601)를 @timestamp로 지정
        date { match => ["timestamp", "ISO8601"] target => "@timestamp" }
    }

    if [@metadata][kafka][topic] == "secure-agent-events" {
        # 포렌식 페이로드의 ts(ISO8601)를 @timestamp로 지정
        date { match => ["ts", "ISO8601"] target => "@timestamp" }
    }
}

output {
    if [@metadata][kafka][topic] == "app-logs" {
        elasticsearch {
            hosts => ["elasticsearch:9200"]
            index => "app-logs-%{+YYYY.MM.dd}"
        }
    }

    if [@metadata][kafka][topic] == "user-activity" {
        elasticsearch {
            hosts => ["elasticsearch:9200"]
            index => "user-activity-%{+YYYY.MM.dd}"
        }
    }

    if [@metadata][kafka][topic] == "exam-events" {
        elasticsearch {
            hosts => ["elasticsearch:9200"]
            index => "exam-events-%{+YYYY.MM.dd}"
        }
    }

    if [@metadata][kafka][topic] == "exam-results" {
        elasticsearch {
            hosts => ["elasticsearch:9200"]
            index => "exam-results-%{+YYYY.MM.dd}"
        }
    }

    if [@metadata][kafka][topic] == "exam-analytics" {
        elasticsearch {
            hosts => ["elasticsearch:9200"]
            index => "exam-analytics-%{+YYYY.MM.dd}"
        }
    }
    if [@metadata][kafka][topic] == "secure-agent-logs" {
        elasticsearch {
          hosts => ["elasticsearch:9200"]
          index => "secure-agent-logs-%{+YYYY.MM.dd}"
        }
    }
    if [@metadata][kafka][topic] == "secure-agent-events" {
        elasticsearch {
          hosts => ["elasticsearch:9200"]
          index => "secure-agent-events-%{+YYYY.MM.dd}"
        }
    }
}